return (
    <div className="fixed inset-0 bg-black/30 backdrop-blur-sm flex items-center justify-center z-50">
      <div className="w-full max-w-3xl h-full mx-4 bg-white rounded-xl shadow-2xl overflow-y-auto">
        {!showSuccess ? (
          <div className="p-6">
            <div className="flex items-center justify-between mb-6">
              <div className="flex items-center gap-3">
                <UserCheck className="text-blue-600 h-7 w-7" />
                <h2 className="text-2xl font-bold text-blue-600">Meeting Details</h2>
              </div>
              <button onClick={closeDetails} className="p-1 hover:bg-gray-100 rounded-full transition-colors">
                <X className="h-6 w-6 text-gray-500" />
              </button>
            </div>

            <div className="space-y-6">
              <div className="bg-blue-50 p-4 rounded-lg">
                <h3 className="text-lg font-semibold text-blue-800 mb-3">Interviewer Information</h3>
                <div className="grid gap-2 text-sm">
                  <div className="flex gap-2"><span className="font-medium text-gray-700">Name:</span>{formData.interviewerName}</div>
                  <div className="flex gap-2"><span className="font-medium text-gray-700">Email:</span>{formData.interviewerEmail}</div>
                  <div className="flex gap-2"><span className="font-medium text-gray-700">Date:</span>{formData.scheduledDate}</div>
                  <div className="flex gap-2"><span className="font-medium text-gray-700">Time:</span>{formData.startTime} - {formData.endTime}</div>
                </div>
              </div>

              <form onSubmit={handleFormSubmit} className="space-y-4">
                <div className="flex items-center gap-2 mb-4">
                  <UserCheck className="text-blue-600 h-5 w-5" />
                  <h3 className="text-lg font-semibold text-gray-800">Candidate Details</h3>
                </div>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  {["candidateName", "candidateEmail", "candidateLinkedIn", "jobTitle"].map((field, index) => (
                    <div className="space-y-1" key={index}>
                      <label className="text-sm font-medium text-gray-700">
                        {field.replace("candidate", "Candidate ").replace("job", "Job ").replace("LinkedIn", "LinkedIn Profile")} *
                      </label>
                      <input
                        type="text"
                        name={field}
                        value={formData[field]}
                        onChange={handleChange}
                        className={`w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 outline-none ${formErrors[field] ? 'border-red-500' : 'border-gray-300'}`}
                      />
                      {formErrors[field] && <p className="text-red-500 text-xs mt-1">{formErrors[field]}</p>}
                    </div>
                  ))}
                </div>

                <div className="space-y-1">
                  <label className="text-sm font-medium text-gray-700">Job Description *</label>
                  <textarea
                    name="jobDescription"
                    value={formData.jobDescription}
                    onChange={handleChange}
                    rows={4}
                    className={`w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 outline-none ${formErrors.jobDescription ? 'border-red-500' : 'border-gray-300'}`}
                  />
                  {formErrors.jobDescription && <p className="text-red-500 text-xs mt-1">{formErrors.jobDescription}</p>}
                </div>

                <div className="flex gap-3 pt-4">
                  <button type="submit" className="flex-1 bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors flex items-center justify-center gap-2">
                    <Send className="h-4 w-4" /> Submit
                  </button>
                  <button type="button" onClick={closeDetails} className="flex-1 border border-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-50 transition-colors">
                    Cancel
                  </button>
                </div>
              </form>
            </div>
          </div>
        ) : (
          <div className="p-6 max-w-md mx-auto text-center">
            <CheckCircle className="h-12 w-12 text-green-500 mx-auto" />
            <h3 className="text-xl font-bold text-gray-800">Request Sent Successfully!</h3>
            <button onClick={handleClosePopup} className="w-full bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors mt-4">Close</button>
          </div>
        )}
      </div>
    </div>